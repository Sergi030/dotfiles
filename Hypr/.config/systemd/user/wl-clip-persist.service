[Unit]
Description=Persist Wayland clipboard across app exits
After=graphical-session.target
PartOf=graphical-session.target

[Service]
Type=simple
ExecStart=/usr/bin/env wl-clip-persist --clipboard regular
Restart=always
RestartSec=1

[Install]
WantedBy=graphical-session.target
